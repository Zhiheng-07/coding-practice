# 项目日志

> 追加式流水账，日期倒序。每次学习/开发活动记一条：做了什么、产出在哪、关键收获。

---

## 2026-08-04 · 模块 03 学习站分区视觉重构：从超长文章到分层可视化

**做了什么**：响应三点反馈（ASCII 图横向溢出、像读超长文章、缺可视化），把模块 03 分区从 md 直转裸 HTML 重构为组件化呈现：①总览 ASCII → 可点击九阶段流水线（节点 chips + QA 并行泳道）；②71 节点 + AI 规范 8 条 → 默认折叠卡（折叠条带门禁摘要），六要素键值排版；③AI 闭环 / 缺陷流转 → 彩色流程链；④三条纪律 → 三列卡片；⑤全站 pre 溢出防御 + 9 张表滚动容器。生成脚本升级为 regen-module03.js（md 仍是单一事实源，可重复再生）。md/Obsidian 未动——三处同步指内容一致，呈现可异（模块 01/02 先例）。

**产出**：学习站模块 03 分区 v2 呈现；模块 CHANGELOG 呈现条目；验证含模块 01/02 分区逐字节比对。

**关键收获**：①学习站早有完整组件体系（step-card/deep-dive/callout/badge），模块 03 初版没用是「能跑就行」的偷懒——同一内容，裸标签和组件化的可读性差一个量级；②长文档网页化的关键是**渐进披露**：总览层（流水线）→ 阶段层（卡片）→ 节点层（折叠），读者按需下钻而不是被迫通读；③溢出这类适配问题要做全站级防御（pre/table 兜底规则），不是逐处打补丁。

---

## 2026-08-04 · 模块 03 建立：研发全流程全景地图 v1.1 + AI 编程协作规范

**做了什么**：① 用户 7 月成稿的《研发全流程全景地图》（九阶段 60+ 节点）经 codex 评审后正式入库为模块 03。对评审 8 条主要意见先逐条核对原文再裁决：**吸收 6 条+部分子项**（经验数字全局标注并降级「铁律」措辞、并行与反馈回路说明、质量「主导权交接/共担」表述、附录 A 补齐 14 角色+RACI 图例、Severity/Priority 三轴注、GPL/单向依赖/QPS 加适用条件、生产工程全量补强——CI 供应链安全/数据治理/备份恢复演练/值班 Runbook/事故分级/功能下线+新附录 F 生产就绪清单），**驳回 2 条不实**（「SDET 行重复」核对不存在、「PM 歧义」文档内已消解），评审第 3 条（AI 内容不足）独立成文。② 新建《AI编程协作规范.md》：8 条规范 + 工作闭环 + 与地图节点对应表。③ 学习站新增模块 03 分区，三处同步闭环。

**产出**：`03-研发全流程/`（全景地图 v1.1 + AI 协作规范 + CHANGELOG + 素材/评审建议归档）；学习站模块 03 分区；tag `rd-v1.1`；Obsidian 同步。

**关键收获**：①外部 AI 评审要「先核对再吸收」——本轮评审 8 条里有 1 条幻觉（不存在的重复行）、1 条打在文档没有的表述上（稻草人），照单全收会引入错改；②用户定调裁决标准：**内容质量是唯一衡量标准，不以改动大小取舍**——生产工程补强改动最大，恰恰是评审里含金量最高的部分；③评审自身也会自相矛盾（一边说「别再加节点」一边开出最长的新增清单），裁决时要看单条内容本身而不是评审的整体口径。

---

## 2026-08-01 · 模块 02 v2.1：外部评审吸收——双通道 review 的第一次实战

**做了什么**：用户用 codex 对 v2.0 手册出具评审报告（12 条意见），我方按「学习材料 + 低风险 MVP」定位逐条裁决：**吸收 7 条**（阶段 4 新增险级三档分级与安全底线 checklist、AI review 边界声明、阶段 3 数据环境隔离与 CI 最小形态、阶段 6 数据回滚盲区、AC 测量条件、适用范围声明与两处绝对化措辞修正），**拒绝 5 条**（完整 NFR/运行时契约/企业测试矩阵/事故管理全套/逐条官方链接——商用系统所需，超出当前学习路径），取舍理由记入 CHANGELOG。评审报告归档至模块素材目录。

**产出**：模块 02 v2.1（md + 学习站分区 + CHANGELOG）；`素材/评审报告-v2.0.md`；tag `mp-v2.1`；Obsidian 同步。

**关键收获**：这轮就是手册阶段 4「双通道 review」的现场演示——外部 AI 评审找出了作者盲区（安全内容缺失是真的），也犯了评审者的典型错误（拿商用生产规范的尺子量学习材料）；裁决权在人，裁决标准是「读者三个月内会不会撞上」。吸收外部评审的正确姿势不是全收或全拒，而是逐条对照目标定位裁决并留下取舍记录。

---

## 2026-08-01 · 模块 02 重写 v2.0：评估式 → Vibecoding 标准开发手册

**做了什么**：用户反馈 v1.0 被其原始认知链路的思路带偏（v1 以「评估用户认知」为主线）。v2.0 放弃该主线，从 AI 产品经理视角正向梳理：场景 = Vibecoding（用户提需求/评审、AI 写代码），对标大厂标准研发规范。主线改为七阶段手册，每阶段固定五件套（目标/产物/分工/Gate/模板 checklist）：①需求定义（PRD 即 prompt + AI 反向质询）②技术方案（Design Doc + 决策记录 ADR + 两大选型 decider）③工程基建（脚手架 + 合规并行线启动）④迭代开发（任务卡 + 单任务五步循环 + 双通道 CR + DoD，全书核心）⑤质量保障（测试金字塔落地 + bug 四步规程）⑥发布（提审 checklist + 灰度 + 回滚）⑦运维与迭代（监控 + 复盘回环）；另增横切章「十条军规」、新自测 9 题、术语表扩充流程词条。学习站 module-mp 分区程序化整块替换，模块 01 分区逐字节不变。

**产出**：`02-微信小程序开发全流程/` v2.0（md + CHANGELOG）；学习站模块 02 分区 v2.0；tag `mp-v2.0`；Obsidian 同步。

**关键收获**：①流程文档的定位不是「评估用户认知」而是「可直接照做的闸门清单」——质量靠闸门不靠天赋，正是它支撑无代码基础的 PM 产出专家级工程；②学习站扩展的正确姿势是「程序化整块替换 + 模块 01 逐字节比对」，既有内容零风险。

---

## 2026-08-01 · 模块 02 建立：微信小程序开发全流程 + 学习站合并

**做了什么**：① 以用户对小程序开发链路的原始认知为素材（"框架规范→架构→选型→规范→编码→测试→改bug→测试→服务器部署→上线"），产出模块 02 教学内容：逐项认知评估表、八阶段全景地图（补上缺失的头——产品定义、合规线——认证/备案/隐私指引、尾——监控与迭代）、两大选型分叉的判断框架（云开发 vs 自建服务器、原生 vs 跨端）、「专家级代码」在 AI 协作模式下的真实含义；时效性事实（认证年审制 30/300 元、备案 7–20 工作日、云开发免费体验环境发布后 15 天到期、Skyline 现状）经网络核对后写入。② 实施 CLAUDE.md 预埋的学习站方向：模块 01 单页 HTML 程序化升级为根目录《Coding实战学习站.html》（顶栏模块切换 + 深链接 + 双词库术语系统 + 分模块自测题 + 后端形态决策参考小工具），模块 01 正文逐字节迁入（已校验），独立 HTML 退役删除。③ 路由文件（CLAUDE.md / 项目进度 / 项目日志）与三处同步铁律表述同步更新。

**产出**：`02-微信小程序开发全流程/`（md 镜像 + CHANGELOG + 素材）；根目录《Coding实战学习站.html》（约 97 KB，双模块）；tag `mp-v1.0`；Obsidian `Coding实战/02-微信小程序开发全流程/` 新笔记。

**关键收获**：小程序开发链路里影响最大的不是技术难点，而是两条「非代码线」——合规线（周期最长、要最早并行启动）和选型分叉（云开发直接决定"服务器部署"环节存在与否）；「专家级代码」对无代码基础者 = PRD 质量 × 评审四问 × 流程纪律，全部是 PM 能力的延伸。

---

## 2026-08-01 · 仓库基础设施升级：零背景开工 + 模块独立化（PR #10）

**做了什么**：① CLAUDE.md 重写为「路由入口」——背景内置（新 session 用户只输任务）、文件路由表（日志默认不读、超 300 行归档）控制 token；② 模块 01 独立化：目录重组为 `01-Git标准开发流程/`（HTML + md 完整镜像 + CHANGELOG + 素材/），确立「三处同步铁律」（HTML / 仓库 md / Obsidian 一致才算完成）；③ 版本记录采用 CHANGELOG + git tag 方案（否决嵌套 git init：模块已在仓库内，Git 天然记录文件历史，嵌套仓库会被外层当黑盒）；④ 项目进度瘦身为纯状态快照；⑤ 同步至 Obsidian vault `Coding实战/` 一级领域目录；⑥ 记忆目录迁移到新路径。

**产出**：PR #10；tag `git-v1.0`、`git-v1.1`；Obsidian 新笔记。

**关键收获**：CLAUDE.md 每次 session 自动加载，是「零背景开工」的天然注入点；版本管理不需要为「想要版本记录」而嵌套 git——CHANGELOG（人读）+ tag（里程碑）+ 仓库自身历史（机器可查）三层已覆盖全部需求。

---

## 2026-08-01 · 三轮澄清补进教学站深入区（PR #9）

**做了什么**：把 8/1 三轮问答的澄清内容补进《Git标准开发流程.html》：步骤 1 新增「clone 和 pull 是什么关系」深入区（买房/收快递类比 + 对照表）；步骤 2 的指针深入区追加「branch 不是版本记录本身」易错点，三层级深入区追加「fork ≠ clone」警示卡与「99% 只用 branch」的拆解验证表，并新增「worktree 主动并行」「fork 完整工作流与 fork 落后问题」两个深入区；术语词库补充 upstream。

**产出**：教学站深入区从 3 个扩充到 6 个（步骤 1–2 区域），单文件约 84 KB。

**关键收获**：教学内容的迭代方式和代码完全一样——发现理解盲区（问答）→ 沉淀素材（问答记录 md）→ 更新产物（教学站）→ 走 PR 合并，每一环都有落盘产出。

---

## 2026-08-01 · branch / worktree / fork 三轮追加问答

**做了什么**：对照教学站深入区用自己的话复述三个概念，暴露出三处理解偏差并逐轮校正：① 把 fork 和 clone 混为一谈（fork 是云端→云端的仓库复制，clone 才是下载到本地）；② 以为「clone 一次」意味着仓库只用一次（实际是仓库层绑定建一次，之后每天靠 pull 收增量——「买房 vs 收快递」）；③ 把 branch 当成版本记录本身（实际 commit 链才是历史，branch 只是链上可移动的标签）。另确认 worktree 支持主动并行多分支（不只是临时救急），以及 fork 的双远程同步模型（origin + upstream）。

**产出**：`文档信息/问答记录-branch-worktree-fork.md`（三轮问答完整记录，PR #8）。

**关键收获**：PR 字面意思是「请求维护者把我的改动拉过去」——方向是请对方拉，不是自己推；一致性问题在「上游↔fork↔本地」每一层之间都存在，解法全是同一个动作：把对方新增的 commit 拉过来。

---

## 2026-07-31 · 仓库转型为《Coding实战》

**做了什么**：本地文件夹由 `git-flow-site` 改名为 `Coding实战`；GitHub 仓库同步改名为 `coding-practice`（旧链接自动跳转）；新建 CLAUDE.md（仓库规范）、项目进度.md、项目日志.md，确立「边做边推进」的学习方式与「所有改动走标准流程」的仓库纪律。

**产出**：本文件所在的三件转型基础设施（PR #7）。

**关键收获**：GitHub 仓库改名后旧 URL 会自动重定向，但本地要用 `git remote set-url` 更新地址才算切换干净。

---

## 2026-07-31 · 站点合并为单文件（PR #6）

**做了什么**：排查「发给别人打开是乱码」的问题——根因是只发送了 `index.html`，接收方缺少 `assets/` 里的样式和脚本。将 CSS/JS 全部内嵌进 HTML 合并为单文件；顺手修复复制按钮在 `file://` 场景下剪贴板 API 不可用的问题（降级 execCommand）。

**产出**：`Git标准开发流程.html`（约 76 KB，单文件、离线双击可用）。

**关键收获**：分享 HTML 页面时必须考虑资源完整性——多文件站点要么整目录打包，要么合并单文件；`navigator.clipboard` 只在 HTTPS / localhost 可用。

---

## 2026-07-31 · 教学网站四个功能模块（PR #2–#5）

**做了什么**：按功能拆四个分支依次开发：七步主线内容与误区对照卡（PR #2）、分支生命周期动画 / 冲突模拟器 / 合并策略切换器（PR #3）、术语悬停解释系统与自测判断题（PR #4）；#4 合并后自检发现术语标记误入 SVG 导致图内标签消失，开 fix 分支走标准 bugfix 循环修复（PR #5）。

**产出**：教学网站全部内容与交互。

**关键收获**：需求拆小 → 分支短命 → 每个 PR 一条 Squash 记录，历史干净可追溯；发现 bug 后的处理方式和开发新功能完全一样——再走一遍流程循环。

---

## 2026-07-30 · 仓库初始化与站点骨架（PR #1）

**做了什么**：以问答记录为素材规划交互式教学网站；初始化 main（初始 commit：问答记录 + .gitignore）；在 GitHub 创建远程仓库并推送；第一个功能分支完成页面骨架、样式系统与滚动联动的流程进度条。

**产出**：仓库主线与站点骨架（PR #1）。

**关键收获**：第一次完整跑通「建远程 → push -u 绑定 → 开分支 → PR → Squash 合并 → 删分支收尾」的全流程。

---

## 2026-07-30 · Git 工作流问答学习

**做了什么**：与 Claude 进行四轮问答，从「拉取/提交/推送/合并是什么」问到七步标准流程、分支本质、三方合并逻辑；第三轮用自己的话复述全流程，暴露并校正了四处典型误区（pull 拉的是完整历史而非单个文件、开分支为隔离而非兜底、Git 不负责环境、push 前无需预合并）。

**产出**：`文档信息/问答记录.md`（原始学习素材）。

**关键收获**：用自己的话复述再被校正，是暴露理解偏差最有效的方式——这四处误区后来成为教学网站「误区对照卡」和自测题的素材。
